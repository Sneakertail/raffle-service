# raffle-service

Raffle service repo extracted from the Sneakertail monorepo.

## Contents

- `src/`, `package.json`, `Dockerfile`
- `.github/workflows/` for CI placeholders
- `k8s-manifests/` for direct Argo CD or `kubectl` usage
